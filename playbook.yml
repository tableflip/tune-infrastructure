- hosts: all
  roles:
    - common

- hosts: dev
  roles:
    - mongodb
    - nginx
    - node
    - meteor
    - frontend
  vars:
    domain: dev.tune.tableflip.io
    root_url: "https://{{domain}}"

- hosts: next
  roles:
    - mongodb
    - letsencrypt
    - nginx
    - node
    - meteor
    - frontend
  vars:
    domain: next.tune.tableflip.io
    root_url: "https://{{domain}}"

- hosts: production
  roles:
    - mongodb
    - letsencrypt
    - nginx
    - node
    - meteor
    - frontend
  vars:
    domain: tune.tableflip.io
    root_url: "https://{{domain}}"
